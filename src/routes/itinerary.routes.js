import { Router } from "express"; import { verifyJWT } from "../middlewares/auth.middlewares.js"; import * as c from "../controllers/itinerary.controllers.js";
const router=Router();router.use(verifyJWT);router.post("/:id/itinerary",c.createItinerary);router.get("/:id/itinerary",c.getItinerary);router.patch("/:tripId/itinerary/:itineraryId",c.updateItinerary);router.delete("/:tripId/itinerary/:itineraryId",c.deleteItinerary);router.post("/:id/itinerary/generate-ai",c.generateAiItinerary);router.post("/:id/itinerary/regenerate-ai",c.regenerateAiItinerary);export default router;
